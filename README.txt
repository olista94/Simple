Analizador Lexico de C--

$ lex simple.l -> para generar el código fuente del analizador en el archivo lex.yy.c
$ gcc -o simple lex.yy.c -> para compilar el código fuente del analizador
$ ./simple prueba.sim -> para probar
